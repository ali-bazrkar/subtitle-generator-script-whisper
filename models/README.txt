i store local models here, make folders for models you wanna download
if you prefer to keep the locally on the same folder instead of your
cache folder which uv handles automatically.

git clone https://huggingface.co/deepdml/faster-whisper-large-v3-turbo-ct2 [DESTINATIO_FOLDER]

git clone https://huggingface.co/Systran/faster-whisper-small.en [DESTINATIO_FOLDER]

i used these two models (ctranslate2 format)
