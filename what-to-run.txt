#you might want to use venv
uv venv
# UV handles stuff really well but i personally still prefer virtual envy
# so files i download belong only to that project and i would recommend it
# i use the "models" folder to keep models locally there you can allow the
# script to download it from the source. i specifically use it because i
# prefer to keep the files on my sight rather than home cache or other
# directories so i can easily see them and manage them when working with things

# cd to current directory to run the python script
# run this command to generate subs with highest quality with GPU/CUDA
$ uv run subgen.py <destination>

# use a weaker / faster model
$ uv run subgen-small-en.py <destination>
